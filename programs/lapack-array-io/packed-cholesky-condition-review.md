# Ordinary packed positive-definite condition candidate

PPCON4 is implemented externally and remains unregistered. The eleven
candidate04 files follow [the frozen source admission](packed-cholesky-condition-admission-review.md).
Audit `0d144ca4b0a82a88c67a6164c44e110a1b7d67997a55988796f0842cff637224`
and its passing seal bind96 original records and66 fresh adapter/test/support
objects. Six GNU Debug/Release and ASC-only Clang19 sanitizer runs each execute
nine tests: five pass and four required mathematical tests fail, with zero
skips and no sanitizer diagnostics. Foreign archives remain unsanitized.
Exact65-primary-TU prior production is explicitly reused and rehashed in each
lane. The actual1116-file candidate comprises v25's1105-file base plus11 owned
files; inherited archive metadata describes the base, not a new archive.

The adapter calls actual S/D/C/Z PPCON, using immutable ordinary packed
factors and a finite nonnegative original norm. Active row factors use p live
caller packing entries; column layout stays direct. Real workspace is3N
scalars plusN native INTEGER, complex is2N scalars plusN underlying-real.
Plans bind every structural option and the active norm branch. N0 and
positive-order zero-norm calls return RCOND1/0 locally, with absent native INFO
and no factor/workspace reads. Source counts cover both LATPS traversals.
INFO starts signedMIN and RCOND starts quiet NaN before entry. Any nonzero,
missing or partial INFO is a defect; negative RCOND is a defect, nonfinite
RCOND is an accuracy warning/partial, and direct outputs remain visible.
No implicit scaling, allocation, fallback, factor certificate or native credit.

Each lane passes1504 independent public workflows, checking diagonal inverse
norms and real/complex Hermitian two-by-two conditions across both triangles,
layouts, seven factor exponents, N0/1/2/3/5, zero norms and repeated plans.
There are247760 LP64 or241936 ILP64 checks: the5824 difference is exactly the
additional native INTEGER bytes inside active work regions, excluded from
per-byte guard checks. Every byte outside the actual work spans is checked.
The608 real/fault/preflight/allocation profiles pass6176 checks. They bind
all native argument pointers, UPLO/N/norm/hidden length, full INFO, negative/
missing/nonfinite outputs, norm validation, stale plans, capacities/alignment,
host placement, operand/workspace/metadata overlaps and dirty-report semantics.
Queries and real calls observe zero C++ and wrapped static-libc allocation.

Linux tests pass192 protected-memory profiles:96 queries with complete A,
RCOND and scratch arrays inaccessible,64 local returns with A and optional
scratch still protected,96 stale-plan and288 invalid-norm rejections while all
numeric arrays are protected, and96 native calls with factor spans adjacent
to inaccessible pages at either boundary. Real containing arrays are created
by C++20 placement array new; no fabricated large backing descriptors are
used. Whole-factor and complete backing guards are retained. These are scoped
read/allocation observations, not full memory/concurrency/platform closure.
Source counts pass19209 checks and four complete private signatures pass.
Current16 strict checks, four normal/no-exception headers and11-file format pass.

## Required mathematical failures

Each of four scalar tests has144 checks with12 failures: both layouts and
triangles at the two smallest subnormal norms and maximum finite norm.
The one-by-one positive-definite matrix has exact reciprocal condition one.
Its rounded square-root factor passes a widened reconstruction check.
Subnormal cases return RCOND0; maximum cases return infinity. Direct pinned
calls reproduce every result with INFO0 in both integer ABIs and all modes.
Unit, minimum-normal and max/4 controls pass. The64-epsilon condition-one
requirement is unchanged for every scale. These are ordinary failing CTests;
argument agreement does not substitute for independent mathematical success.
The source zero/underflow guard and final reciprocal ordering explain the
failure paths. No provider patch, rescaling, fallback or waiver is authorized.

Candidate01's public fixture has two identical zero-value branches; both
strict checks fail. Candidate02 combines them with all workflows/assertions
unchanged and adds fault tests. Its external harness omits the dense-test
include directory, so both builds fail; four strict checks also require
nodiscard and matching parameter names. Candidate03 fixes those issues and
adds memory tests; both Debug5/5 and all19 static records pass. Candidate04
adds the required scalar extremes without changing the preceding ten files.
Its six runs retain four mathematical failures each; both new strict checks
and full formatting pass. All96 raw records remain, including14 nonzero
records; no failed build receives numerical credit. The first audit used the
LP64 byte-guard check count for ILP64; audit02 corrects the exact count using
the5824-byte derivation, with no test change. Original audit/log and partial
inspection outputs are retained and rehashed. Both audit executions are
preserved separately from the96 normalized command records.

These96 records plus the passing seal add97 to the ledger, now4002; source
admission's15 records were already normalized. Root PPEQU v25 remains active.
After its root/default/docs audit and local commit, import this exact11-file
PPCON candidate, register its four ordinary required mathematical gates, and
run fresh root integration. Full routine/mode/concurrency/platform acceptance,
all P00-P11,70 existing andfour new required mathematical tests, and130 missing
required XBLAS remain open. Zero routines are fully verified.
