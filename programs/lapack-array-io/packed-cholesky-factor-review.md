# Ordinary packed Cholesky factorization

The nine candidate03 files add actual S/D/C/Z PPTRF, metadata-only queries,
source-specific integer admission, public independent mathematics and failure
acceptance. The explicit optional provider and existing packed BLAS descriptor
preserve C++20, both packed layouts and the six-module CPU boundary. Row
conversion uses p=N*(N+1)/2 live caller-owned scalar objects. No densification,
implicit conversion, precision change, allocation or provider patch is added.
Native LU/Cholesky/Householder QR remain twenty separate partial contracts.

## Source and partial output

[The preserved admission review](packed-cholesky-admission-review.md) records
source integer endpoints and the ten precision source pairs. The exact four
PPTRF roots now each have complete GNU-emitted signature comparisons in both
ABIs, including the one hidden CHARACTER length. The factor-only mechanical
closures contain22 objects per ABI, no writable static symbols and no external
imports after conditioning XERBLA. Root options and every selected transitive
BLAS argument exclude that diagnostic edge: upper TPSV uses N=J-1>=1,
transpose/conjugate/nonunit with increment1; DOT uses J-1>=0; lower SCAL/HPR/SPR
use N-J>=1 with increment1 and lower storage. LSAME receives valid characters.

The selected PPTRF, TPSV, DOT/DOTC, SCAL/real-SCAL, SPR/HPR loops are bounded
by validated integer controls, including final cursor increments. They have
no data-dependent retry loops or foreign workspace queries. NaN/Inf arithmetic
cannot alter their finite loop counts; it does not become a mathematical pass.
No foreign allocation import remains in the admitted22-object closure. Actual
initial/repeated selected static-link calls additionally observe zero C++ and
wrapped libc allocations; shared runtime internals and other platforms are
outside that observation.

Complex diagonal packing reads only real components. On upper INFO=j, columns
through j are published; future columns stay unchanged. Lower INFO=1 publishes
only the first diagonal. Later lower failures retain all source-defined trailing
updates, including imaginary diagonal clearing by HPR. Positive INFO returns
kNumerical/kNotPositiveDefinite/kDocumentedPartial and index INFO-1. A MIN
sentinel detects missing/low-half INFO writes; negative or >N INFO is a provider
defect. Row defect publication is withheld while direct writes cannot roll back.

## Executed evidence and remaining gates

Candidate03 audit
13a3e47cb1aed1474af1df2e4b68f0b1e214d07756b3bc263ec53349de41f325
and its completion record bind95 original records and48 fresh final candidate
objects. Six final configurations each pass4/4 tests with zero skips; the
65 prior primary ASC translation units per lane are explicitly reused and
rehashed. Foreign archives remain unsanitized. Twelve exact-input strict,
four isolated normal/no-exception public-header and current9-file format
checks pass. Candidate02's two strict failures remain preserved; candidate03
only names an unused test parameter in a comment, preserving all assertions.

Each lane passes688 public workflows/16,017 checks: four scalars, order0/1/2/3/5,
both triangles/layouts, seven even dyadic exponents including minimum-normal
and near-maximum finite cases. Independently enumerated physical slots and
known positive-diagonal factors support widened max-entry reconstruction
and factor comparisons, with epsilon/dimension-based bounds. First/last
nonpositive pivots preserve exact source partial outputs and all guards.
Each lane also passes384 failure/allocation profiles/3,088 checks,12,340 pure
source-count checks and four complete private signatures. Injected native
writes prove row defect withholding and direct-write survival; injected INFO
is diagnostic evidence, separately from real nonpositive factorization.

All32 packed positive-definite routines remain required. Only PPTRF4 becomes
in_progress, bringing the mapping to326 Reference in_progress/1787 not_started;
zero fully verified and native20 implemented_unverified. Full legal mode,
ignored-load/concurrency/platform acceptance, remaining PP families and all
P00-P11 outcomes remain open. The executed root integration is recorded below. Existing70 full-suite mathematical failures and the
130 missing required XBLAS routines remain visible and required.


## Executed v21 integration

Audit `893e9fd086efbbf96b563256653e03a350f72038f4cfef8b9c37ebf73a320958`
binds the 1064-file frozen product, 30 raw records and completion seal. Both
GNU Release suites execute 888 tests: 818 pass and 70 required mathematical
tests fail. Both affected Debug suites pass 68/76 with eight mathematical
failures; both ASC-only Clang19 sanitizer suites pass 17/25 with the same
eight failures. There are zero skips and no sanitizer diagnostics. All 66
Core/Dense/provider TUs compile freshly in each lane: 396 primary objects.
Foreign archives remain unsanitized. Four relocated packages execute all 25
family consumers, including PPTRF's 688 workflows and 16,017 checks. Six
static checks pass; Doxygen covers 120 headers and 2,156 public members with
zero warnings. Candidate strict/header checks bind unchanged compiler inputs.

The first root audit failed because its count assertion still expected 56
primary TUs. The preserved audit-02 checks the actual 66 source set, build
commands, objects and archive member hash multisets; no test changed.
Provider-free audit
`88421ada128c18ef95c257d2816ddda9f147add6e451b50e0c4c54d947f20123`
binds fresh static/shared 16/16 checks each, 60 primary objects and 12 support
objects. Optional headers are absent and foreign discovery/linkage is absent.
These are scoped baseline checks, not full baseline or shared-provider proof.
The 40 new raw/completion records bring the normalized ledger to 3,212.

Full routine/mode, physical ignored-load, concurrency and platform acceptance
remain open. PPTRS4's candidate is separate and unregistered; all 2113 required
Reference routines, native20 and complete P00-P11 outcomes remain required.


The final three documentation changes pass six fresh checks. Documentation
audit `7212ad50b2224f1c3ae415deeb66325a5eda22694753a97c06cf2c1269370fcf`
rehashes 9,499 unchanged numerical/default compiler inputs and artifacts,
including installed consumers. Its archive is
`12abe0777b2cd73a4f849a987da0d8d221bcfd7c906eaf3e0f90b521f0cb39ff`;
the numerical executions retain their earlier v21 identity. The ledger now
contains 3,218 normalized records. No numerical rerun on prose is claimed.
