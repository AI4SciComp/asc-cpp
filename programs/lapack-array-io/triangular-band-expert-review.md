# Checked triangular band estimators

The11 candidate03 files implement S/D/C/Z TBCON/TBRFS with explicit band
storage, independent A/B/X layouts, caller scalar/real/native-integer/packing
workspace and surviving full-width INFO. Condition order zero returns one;
error bounds with zero order/RHS return zero locally with absent INFO.
Plans bind six native error dimensions/nine options, or three condition
dimensions/five options, including original strides. Structural rejection
precedes numeric mutation; metadata aliases preserve reports. No densification,
provider patch, hidden rescaling, fallback or allocation is introduced.

[Source admission](triangular-band-expert-admission-review.md) records exact
integer bounds, both81-object closures, finite/NaN SCALE progress and the
reproduced complex LATBS single-term omission. Missing required XBLAS130 and
all2113 Reference operations remain in scope; native20 is reported separately.

## Executed candidate evidence

Audit8eec6b0cf5c973f3f12ed4d8015e0f68582d2426585165d840cbd424da1b09d5
and its completion seal bind64 original records and54 fresh final objects.
Six configurations each execute12 tests:four pass,eight required mathematical
failures,zero skips. Prior64 primary ASC TUs per lane are explicitly reused
and rehashed; foreign archives are unsanitized. Fourteen exact-input strict,
eight explicit isolated public-header and current11-file format checks pass.

Each public suite passes33,280 workflows/555,520 assertions across four
scalars,N=0/1/2/5,KD=0/1/N-1/N/N+2,all layouts/triangles/diagonals,N/T/C,
NRHS=0/3 and dyadic scales. Fixtures independently decode physical band slots,
form known equations and check analytic diagonal RCOND, weighted BERR,FERR,
complete A/B/X preservation, output guards and exact packing capacities.
Each failure suite passes69,888 profiles/394,240 assertions, including all
INFO faults, negative/NaN/Inf/unwritten estimates, initial/repeated calls,
zero observed C++/linked-static allocations and every dimension/option key
mutation. Pure source admission passes71,282 checks; eight complete native
function signatures match. Broader operand-alias/invalid-option crossproducts,
protected-load observations, platform and full-mode closure remain required.

Each scalar condition math test retains17 failures among212 checks: one tiny
scalar and16 maximum-scale exact-condition matrix cases. Error math retains
two failures among25 checks: finite tiny/maximum scalar FERR is infinite.
These are ordinary failing gates, not expected failures or waived tests.
The original candidate02 two build failures and four strict failures remain
preserved; candidate03 fixes only an unused include and test guard span types.

## Root v20 registration

Eleven source/header/test files are imported unchanged. Optional facet source,
header owners/hashes,119-header oracle, isolated-header consumers and24-family
installed profile change atomically. Eight rows become in_progress, yielding
322 Reference in_progress/1791 not_started,zero fully verified. Fresh root
integration and installed evidence are pending. All other P00-P11 work remains
required; the old62 plus new8 mathematical gates remain visible.

## Protected-memory candidate

Candidate02 audit `783533f58dcc65ade1fc5e4639228af13029737ce1f2a01a4ebd1475c83be670`
binds 54 original records and 12 fresh test/support objects. GNU Release/Debug
and ASC-only Clang19 sanitizers execute 1/1 on both ABIs, with 1,056 profiles
per lane and no skips or sanitizer diagnostics. Both strict checks and format
pass. Two original strict failures are retained; the correction only splits
two multiple declarations. Exact candidate03 adapter and v19 production
libraries are explicitly reused and rehashed. Root integration is pending.

Five profile shapes cover unit scalar orders with KD=0/3, empty order and
zero RHS with unit/nonunit diagonals. Both triangles and every A/B/X layout
combine with one/infinity norm or N/T/C. Real C++20 containing arrays are
established before page protection. All A pages are inaccessible where A is
ignored; active B/X values remain accessible while padding pages are protected.
Inactive B/X arrays are entirely protected. Queries and execution succeed,
complete backing bytes and workspace guards survive, and the calls observe
zero C++ allocations. This is scoped Linux load evidence; foreign sanitizer,
full operands, platform and all-mode acceptance remain required.

## Executed root v20 and provider-free integration

Root audit `9becc61154c7bcd71b532ea3bb58d968dd4e660e7bda27f74ed8067f2a9979a7`
binds tree `29060f4e2a9bdb41a843dd2a4f1572f2a1260bea`, archive
`3bc1666c6beac4bfd3c627c6e6cd8467f4a9ba3d665fed0fa79fbbce22727085`
and 30 original records. Each full GNU Release suite executes 880 tests:
810 pass, 70 required mathematical gates fail, zero skip. Each affected Debug
suite executes 68: 60 pass and eight math gates fail. Each ASC-only Clang19
sanitizer executes 19: 11 pass and the same eight fail, without diagnostics.
All 65 Core/Dense/provider TUs compile freshly in each lane, 390 objects.
Four relocated packages each pass 24 families. Six static checks pass;
Doxygen covers 119 headers and 2,148 public members without warnings.
Exact candidate strict/header evidence is bound unchanged. Foreign archives
remain unsanitized; complete mathematics/modes/platform/program gates stay open.

Provider-free audit `49799ecb112ddca0838505c450ec6478035c339ef0fce9b33e1ec6780b69342c`
binds eight passing records: static/shared 16/16 each, 60 fresh primary and
12 test/support objects. Actual installed/relocated prefixes contain the neutral
header and omit the provider facet. No LAPACK/Fortran configuration or runtime
is pulled into these consumers. A premature audit failed because the root audit
was still running; its script/log/failure record and four inspections are kept.
Corrected audit02 pins the completed root audit and revalidates all artifacts.

## Operand preflight candidate

Audit `e4cab89316acda77cecaa957b9c2a98e38f27998ee914ea09e442c58a0f7af0c`
binds 34 original records and 18 fresh test/fault/support objects. Six final
runs pass 1/1 and 28,800 profiles each, with no skips or sanitizer diagnostics.
Both strict checks and format pass. Exact candidate03 adapter and v19 primary
objects are explicitly reused. All four scalars, KD=0/1/4, triangles, diagonals,
A/B/X layouts and N/T/C combine with 25 rejection cases per profile.

The tests cover invalid flags, incompatible shapes/error lengths, strided
error vectors, device placement and A/B/X/FERR/BERR overlaps. They check full
operand/workspace preservation, exact errors, report reset, zero C++ allocation
and no observed native entry. The checked band descriptor rejects device tags
during construction; those cases are tested at that boundary. The original
success-only factory assumption caused one aborted test and is preserved with
two strict failures. The correction retains every rejection case, extracts
small fixture methods, and scopes intentional invalid fixed-underlying enum
checks consistently with existing tests. Root registration remains pending.

## Final v20 documentation binding

Six fresh documentation/contract/source checks pass for tree
`b04f6bc01a7c97c570c89bb65c7fa4df998490ba`, archive
`245a2ee3d40afffe0c6bc0c839869ac229232de7442b4fa8d692ecb2356000ff`.
Audit `ffbad2202d73a82062677fc7249c2cc68f71835403a2bfe5d30973808bfd4107`
rehashes 8,700 numerical/default compiler inputs and artifacts unchanged. Only
three released prose/scope files differ from the executed root snapshot. No
new mathematical execution or closure is claimed for this prose update.

## Registered memory and operand tests

The two audited candidate sources are imported unchanged and registered in
root CMake. Audit `f30886c6a1c0ca0b465ea3bddf329a01f6556f05da0f692ba0e604b46f9e6d17`
binds tree `36c0541f18a7493945be5516dd80ec0a6e9e1940` and archive
`e15f50c06c8ace6f8263ac28aa4dd22f03263003f67cb09cf0ba58eef5456353`.
Four fresh affected GNU Release/Debug suites each execute 70 tests: 62 pass
and eight required mathematical gates fail. Both ASC-only sanitizer suites
execute 21: 13 pass and eight fail. There are no skips or sanitizer diagnostics.
Every lane freshly compiles all 65 primary TUs, giving 390 objects; all four
relocated 24-family packages, four current-root strict checks and format pass.
The 1,056 protected-memory and 28,800 operand-preflight profiles execute in
every lane. Prior full v20 inputs and 70 mathematical failures are rehashed
without relabeling the older 880-test executions as fresh full-suite results.
No public declaration, provider identity, production code or frozen contract
changes in this three-file test slice. Stronger modes/platform and full P00-P11
acceptance remain required.
