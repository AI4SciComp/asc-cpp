# Ordinary packed positive-definite condition admission

PPCON4 remains required and unregistered. Audit
`284c4314cf5077ca9fe33d19522ae2992e80c6e55ac5fc7c5f0204aee857409c`
and its passing seal bind14 raw records plus the seal: six passing pure-count
configuration/build/test/strict/format records andeight complete source/ABI
records including the retained first precision-comparison failure. The fresh
GNU11 Debug C++20/no-exception test passes19,209 literal cursor and actual
LP64/ILP64 endpoint checks. One test object is fresh; exact v22 Core is reused.
Counts use1107 files (v25 base1105 plus two additions), whereas the earlier
source proof usesv24's1095-file snapshot. Each identity is checked separately;
base metadata does not claim a new archive. The ledger now has3,905 records.

The first audit execution looked for record-precision-01, but the actual
retained failed command is record-precision. Audit02 corrects only the lookup;
its seal rehashes the original audit and failed execution log. No numerical
assertion, failure record or proof requirement changes. The source review
below is bound by the audit; it establishes no typed or runtime capability.

# PPCON source admission self-review

Scope: pinned Reference-LAPACK 3.12.1, GNU11.4 Linux x86_64 static LP64 and true
ILP64. This is root self-review and source progress reasoning, not independent
approval, numerical execution or a condition-number accuracy certificate.

The complete D/Z PPCON bodies and S/D,C/Z statement-normalized pairs are read.
The first precision comparison omitted LACN2/LATPS/LAMCH/RSCL symbol mappings;
its two diffs and failed record remain. The second maps these precision names,
preserving every executable statement and branch label; both complete pairs
match. Eight complete emitted nine-argument function types (eight explicit,
one hidden CHARACTER size_t length) match private installed declarations.
Each closure has60 objects. Its56 non-PPCON objects are byte-identical to the
previous81-object packed triangular expert closure, whose complete LATPS,
LACN2, RSCL, selected BLAS and machine-constant reviews are explicitly bound.
No LATRS or LANTP algorithm is substituted for LATPS.

PPCON validates UPLO,N and ANORM>=0, sets RCOND0, then returns1 for N0 or0 for
ANORM0. Active real workspace is3N scalars plus N native INTEGER; complex uses
2N complex plus N real. ISAVE3 is local, KASE starts0. The two LATPS calls
use opposite transpose orientations: upper U^T then U (complex U^H then U),
lower L then L^T/H. They solve through the given triangular Cholesky factor;
complex factor imaginary diagonal components are coefficients and are read.
NORMIN startsN, then remainsY, reusing CNORM within the estimator iteration.
LACN2 ignores no required inverse operation based on KASE: the represented
matrix is symmetric/Hermitian. Its state1..5 and ITMAX5 bound all retries;
real NINT arguments are assigned exactly+1/-1 before conversion, even for NaN.

Integer admission protects the actual predivision N*(N+1) and3N expressions.
This covers LATPS/TPSV both packed orientations, lower KC+N before subtraction,
ascending terminal p+N+1 and descending -N, N+1 loop exits and real WORK(2N+1)
through3N. For N>=2 the predivision product bounds p+N+1; at N1,3N does.
The pure oracle walks these statements twice for the two solves, independently
accumulating the product and vector segments. It does not allocate fabricated
near-limit backing arrays. LP64 endpoint46340 and ILP643037000499 are integer
bounds; checked ASC byte-span admission is a separate requirement.

PPCON passes SCALEL*SCALEU to RSCL after its zero/underflow guard. An infinite
RSCL denominator would not progress, so the guard alone is insufficient.
The rehashed LATPS proof covers NORMIN N and Y, both triangles and all used
transpose modes. With finite TMAX, SMLNUM=safe_minimum/precision and
TSCAL=1 or a positive reciprocal bounded so1/TSCAL<2^26(binary32) or2^55(binary64),
including the complex HALF factor. SCALE starts1; every internal scale update
is nonnegative and <=1 or NaN. Thus the returned SCALE/TSCAL is nonnegative
finite below those bounds, or NaN. With TMAX=Inf,TSCAL=0, the slow-path zero/NaN
diagonal branch assigns SCALE0 and final0/0 produces NaN. NaN TSCAL also gives
NaN. CNORM reuse changes none of these cases.

Consequently the new two-call product is finite nonnegative below2^52 or2^110,
well within binary32/binary64 finite range, or NaN. Underflow to zero takes
PPCON's explicit return guard before RSCL; it cannot introduce Inf. Every
reached RSCL denominator is finite positive or NaN. Positive finite retry
steps reduce numerator/denominator exponents over a finite machine range;
NaN comparisons select the final-ratio branch immediately. Both LATPS loops
and BLAS loops have protected integer terminals. No unbounded progress path
is admitted by arbitrary finite/nonfinite raw factor values in these pinned
arithmetic builds. This source argument does not claim finite or accurate
RCOND, and the existing estimator mathematical failures remain failures.

XERBLA is conditioned only on checked root and transitive arguments. There
are no writable static symbols; remaining imports are cabs,cabsf,memcpy,memset.
Absence of allocator imports is source evidence, not a runtime allocation
measurement. Foreign code remains unsanitized. Full floating-environment,
mode, concurrency and platform acceptance remain open.

The typed path will follow the existing POCON contract: finite nonnegative
original matrix norm, immutable ordinary packed raw factor, disjoint live
underlying-real RCOND, metadata-only query, explicit3N/INTEGER or2N/real work,
p caller packing for active row layout, no packing for column layout andno
implicit rescaling/fallback. N0 andnonempty ANORM0 complete locally withRCOND1/0
andabsent INFO, no factor/workspace read; invalid flags/norm/metadata still fail.
The plan binds routine/scalar/provider,N,triangle,layout andactive norm branch;
changing one positive finite norm toanother may reuse the same plan. Active
source counts precede writes; all workspace andmetadata alias rules persist.
INFO starts signedMIN; anynonzero/missing/partial INFO is a provider defect,
withraw direct output retained. Negative RCOND isdefect;nonfinite RCOND is
accuracy warning/partial,finite nonnegative RCOND complete (zero gives no
fabricated singular-pivot diagnosis). Raw factors carry no provenance or
positive-definiteness certificate. Root public/installed/mode tests, analytic
condition fixtures, extreme-scale failures andstrong read/allocation evidence
remain required before routine acceptance. All2113 routines and130 missing
requiredXBLAS remain required;no native credit is added.
