# P03 Dense binary stream boundary acceptance

Fresh root Debug/Release/shared/ASC sanitizer each3/3;three fresh relocated installed-only Dense consumers each3/3 with exceptions disabled:21passes zero skips.76 fresh Core/Dense objects;33 Sparse/Random/Utilities install prerequisites have no runtime credit. Initial installed harness missing-header build failure retained and fixed in new harness/directory. Prior exact-source strict/three diagnostic lanes rehashed, no full P03/platform/allocation-mode closure. Independent72-byte i16 fixture:75 reads and145 writes (220 workflows per lane), every failing boundary,3-byte short chunks, whole-file EOF failure before commit, exact reports/native diagnostics, whole destination/padding rollback, guarded staging/metadata/scratch, and failed-cursor nonreplay. Other scalars/formats/modes/platforms and complete P03 acceptance remain separate.

Product tree `445a63fed6224249e82a9781e338de710baf4764`, archive SHA256 `c42b0ddf8b9bb424ebfa1e5bacd1374a32c9b64978c644d90b4ea149fc2b9615`. Completion audit `p03-dense-binary-progress-integration-01/audit.json`, SHA256 `b3838127365dc9c7a6c3e048dd31aa558192f9ac82bc86a7743f9306abf98d8f`.

The new io_stream_progress_test.cc is registered beside the existing Dense I/O and fuzz tests, with the existing allocation observer. Production code and frozen wire contracts are unchanged. Root read the complete regression and corrected retry assertions before import, then rehashed all58 prior compiler-read inputs. The72-byte literal fixture also matches the frozen hand-derived Dense i16 file and an independent stdlib CRC32 calculation in candidate04's sealed audit.

The test reads values -2 and513 into a live rank-one strided view. Failed Into calls compare the whole destination object representation, including unused elements. Callback failures retain kIo/native -1729 and empty borrowed diagnostic strings. Header/payload/trailer progress, completed scalar count, commit flag and cleanup error are checked exactly. The source/sink returns at most three bytes per callback; zero-progress writes return kIo without spinning. Framed success at byte72 makes no next-frame callback; the whole-file EOF probe must finish before commit. The query-free cursor retry cannot invoke the source again or change the target.

Preserved candidate03 failed nine assertions that incorrectly required InvalidState after a failed source. Existing Input::Fail retains the original callback error, as required by the frozen bounded diagnostics contract. Candidate04 corrects those assertions to kIo/native -1729 while retaining InvalidState for a previously successful cursor, and adds empty diagnostic checks. It passed GNU11 Debug/Release and Clang19 ASan/UBSan each3/3 on freshly built Core/Dense. The first agent strict failure and incomplete candidate02 remain preserved outside the tree. None of their labels count as acceptance.

Root current registration ran3/3 tests in each GNU11 Debug/Release/shared and Clang19 ASC-only sanitizer lane. Three relocated installed Dense-only consumers each pass3/3 with exceptions disabled, reading only installed public Core/Dense headers. Each of the four root lanes rebuilt all19 Core/Dense translation units. The33 Sparse/Random/Utilities objects required for installation have no runtime/reproducibility credit. The audit binds all151 fresh compilation objects,8 Core/Dense libraries,21 binaries and18 ELF inspections. Old exact-source strict evidence is rehashed; no new root strict invocation is claimed.

The first external installed harness omitted the existing counting_memory_resource.h used by io_test.cc. Its failed build and original harness remain preserved; a new harness includes that exact test header and builds in new directories. The first completion audit guessed an incorrect JUnit filename; audit02 reads the actual preserved ctest.xml and passes without modifying any tests or their logs.

P03 remains in progress. This i16 binary regression does not establish all text/binary scalar/rank/storage/layout crossproducts, owner/resource failure coverage, long borrowed diagnostic string handling, all allocation observers, platforms or path close/cleanup behavior. Existing acceptance for those dimensions remains separately source-bound. The program still requires2113 Reference routines:274 partial,1839 not_started;20 native implemented_unverified and zero fully verified. Missing required130 XBLAS routines and numerical failures are unchanged.
