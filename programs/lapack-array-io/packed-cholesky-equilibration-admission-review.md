# Ordinary packed positive-definite equilibration admission

PPEQU4 remains required and unregistered. Audit
`f8e0165e25a17a2f317e4096a1df5cf659186aaa0950dc704776ff763ed5fb53`
and its executed seal bind 13 passing original records: six pure-count build,
execution, strict and format records plus seven complete source/ABI records.
The final GNU11 Debug C++20/no-exception test passes 12,501 literal source,
virtual-machine, actual endpoint and physical diagonal equivalence checks.
One count TU compiles freshly; audited v22 Core is explicitly reused. Its
1087-file source is v23's 1085-file baseline plus two owned additions. Base
archive metadata does not claim a new archive. The 14 raw/completion records
bring the ledger to 3,644; no typed or numerical capability credit is assigned.

Both complete real and complex entry precision pairs match. Eight complete
GNU-emitted signatures match the private installed declarations, including
seven explicit arguments and one hidden CHARACTER length. Each actual static
provider ABI has a six-object closure with no writable static state or external
imports after valid-argument XERBLA conditioning. Source, archive members,
private headers, runtime attestations and compiler inputs are rehashed.

## Frozen typed behavior

Implement explicit optional-provider S/D/C/Z QueryPpequWorkspace/Ppequ with
immutable ordinary packed A, an explicit selected triangle, contiguous
underlying-real output S and disjoint live real SCOND and AMAX objects. PPEQU
computes S(i)=1/sqrt(real(A(i,i))), AMAX=max(real diagonal), and
SCOND=sqrt(min(real diagonal))/sqrt(max(real diagonal)); it does not apply
scaling or certify positive definiteness. Off-diagonal and complex imaginary
diagonal components are ignored; all A storage remains unchanged.

For this diagonal-only operation, row-upper diagonal offsets exactly match
column-lower, and row-lower match column-upper. Call the actual native PPEQU
on the original storage with the opposite UPLO for row layout. No packing,
densification, complex casts, blanket input scan or hidden allocation is needed.
This routine-specific equivalence does not change other packed operations.
Queries inspect metadata only and bind routine/scalar/provider, N, original
triangle/layout, effective native triangle, S length and increment. Both layouts
require zero workspace; nonempty unexpected workspace still obeys existing
capacity, alignment, alias, placement and live-object validation.

All operand spans, scalar objects and workspace must be disjoint from each
other and provider/plan/report metadata under existing contracts. Metadata
alias rejection precedes report reset. All other preflight and stale-plan
checks precede numerical mutation or provider entry. Zero order completes
locally with SCOND=1, AMAX=0, S unchanged, and no fabricated native INFO.
Synchronous calls with disjoint mutable outputs/reports are reentrant; callers
exclude conflicting access. No process-global provider handler is installed.

Initialize the complete native INFO object with the signed minimum sentinel.
Negative, missing, partial or greater-than-N INFO is a provider defect with
raw direct output retained and unusable. Positive INFO j reports exact
not-positive-definite, index j-1 and documented partial output: every raw real
diagonal has been copied to S and AMAX is complete, while SCOND is unchanged.
The scale vector is invalid on this branch. On INFO0, apply the existing POEQU
output validation: finite positive S, finite SCOND>=0 and finite AMAX>0 are
required; otherwise report numerical accuracy warning with raw partial outputs.
This late output check is not a pre-entry numerical scan. Finite tiny/huge,
zero and nonfinite cases require explicit tests; no implicit rescaling/fallback.

## Source integer and progress obligations

PPEQU validates UPLO/N before reads and has fixed bounded loops without
workspace queries, data-dependent retry, logarithms or float-to-integer casts.
Upper cursor JJ accumulates through p=N*(N+1)/2; lower evaluates the actual
left-associated JJ+N-I+2 expression, reaching p+N-2. Loop exits and the final
scale/nonpositive scan require N+1 to fit. The admitted PPTRF count bound
already covers these expressions, with triangle chosen by effective native
layout. The independent literal oracle models PPEQU statements rather than
repeating the helper formula. Physical-offset checks enumerate both layouts
and triangles for N0 through64 without pretending to allocate huge matrices.

Effective upper endpoints are 65,535 LP64 and 4,294,967,295 ILP64; effective
lower endpoints are 65,534 and 4,294,967,294. ASC backing/byte checks remain
separate. Invalid flags are rejected, including at zero order; no source
narrowing or provider entry occurs for local empty calls. XERBLA is conditioned
only on these checked root arguments; LSAME has no additional integer loops.

Typed adapters, independent diagonal equations/statistics, first/last and
multiple nonpositive entries, all layouts/scalars/scales, malformed metadata,
full-width INFO injection, guarded/protected memory and allocation observations,
root/installed and complete mode/concurrency/platform evidence remain required.
All P00-P11, 70 existing ordinary mathematical failures and 130 missing required
XBLAS routines remain open. No native or fully verified credit is added.
