# Packed Bunch--Kaufman reciprocal-condition contract

Six checked SPCON/HPCON adapters follow inverse checkpoint
`32ef513187a11d69352d948ad1e9f6c878e492ff`. Their engineering gates are complete;
required range mathematics remains failing. Registration uses
`implemented_unverified`, with no numerical verification credit.

Six required source instances match the exact pinned inventory/provider commit
`6ec7f2bc4ecf4c4a93496aa2fa519575bc0e39ca`. All twelve actual GNU LP64/global
ILP64 declarations match normalized native prototypes. Both guarded raw native
prerequisites pass 648 diagonal, paired-block, singular, empty and dyadically
scaled cases, with independently known reciprocal condition numbers and
immutable AP/IPIV plus guarded WORK/IWORK/RCOND/INFO. The initial test-helper
conversion compile failure is retained separately from corrected attempt02.

The public plan binds scalar/provider, order, triangle/symmetry, packed layout,
exact pivot count and the zero-norm branch. Positive original one-norm values
may vary within that branch. Empty order produces RCOND=1 and nonempty zero
norm RCOND=0 without native calls, numerical reads or scratch. Active workspace
is 2*N scalar WORK, N native pivot INTEGERs, plus N real-only IWORK INTEGERs;
row AP adds N*(N+1)/2 simultaneous scalar entries. Full packed products, TRS
one-RHS endpoint and LACN2 3*N bounds precede arithmetic and conversion.

Complete matching SPTRF/HPTRF origin, including completed singular factors,
is required. Raw descriptors cannot prove it. All equal-negative pivot pairs
and directional bounds are validated before numerical mutation. Any exact
scalar-zero positive-pivot1block preserves the source INFO=0/RCOND=0 early
return. Otherwise exactly zero native TRS block divisors reject before scratch
or output mutation. No broad finite-data scan or original-input Hermitian
normalization is applied to factors. Queries observe metadata and the scalar
norm only; all numerical/scratch/live-metadata ranges must be disjoint.

Native INFO is full-width minimum-seeded; it must return zero and native
converted input pivots must remain unchanged before RCOND publication. On that
provider defect, the caller's RCOND is preserved. Otherwise actual native
RCOND is published: finite nonnegative values are complete, nonfinite values
retain an accuracy warning, and negative values are provider-invalid. RCOND
is an estimate, not an accuracy or factorization certificate. Immutable AP and
caller pivots may be shared with disjoint outputs/workspace/reports.

The final sixteen actual LP64/global ILP64 profiles cover static/shared Release,
Debug, ASC-ASan+UBSan and TSan. They select 552 processes: 480 pass, 72 mandatory
range mathematical failures, zero skips. Each normal/sanitizer profile passes
38/44; all four TSan profiles pass6/6. Foreign Fortran/BLAS internals remain
uninstrumented. The maintained tests comprise:

- Ordinary mathematics and separate guarded native fidelity: 288 cases per
  scalar/mode, with both triangles/layouts, empty/singular/paired/diagonal
  fixtures and three dyadic scales.
- Range mathematics and separate native fidelity: 64 cases per scalar/mode.
  Real variants admit64 finite norms; complex variants admit60 and check four
  explicit unrepresentable-norm rejection controls. All six mathematical
  processes fail on admitted tiny finite inputs; all six fidelity processes
  pass. Scalar min-normal/8 has exact reciprocal condition1 but native0.
  Paired tiny blocks can return NaN or0. No case or tolerance is removed.
- Provider faults: 1,088 cases per scalar class across17 faults, including
  missing/truncated/negative/positive/maximal INFO, private-pivot corruption,
  omitted/negative/nonfinite/subnormal/zero/negative-zero/above-one RCOND.
- Validation: 216 rejection cases per scalar class, PROT_NONE no-read tests,
  source singular-scan/divisor precedence and reuse of positive-norm plans
  with separately produced matching factors.
- Concurrency: 96 groups per scalar class, four workers and four repetitions,
  shared read-only protected AP/IPIV, disjoint outputs/workspaces, independently
  checked serial mathematics and stale-plan prewrite rejection.
- Native ABI: 648 guarded cases in each actual emitted GNU integer ABI, plus
  the maintained raw ABI test and pure-count tests at both integer limits.

All four relocated public-only consumers pass576 producer/condition workflows.
Ten strict translation units, four standalone header checks, ten package
manifests, three architecture checks and five public surfaces pass. Each ABI
adds12 exports and removes none. The actual workflow selector and required-test
guard include1741 processes, including all44 condition tests. Initial helper,
validation compile, strict-analysis and stale-new-header-checksum failures are
preserved with their corrections. They do not suppress a failing test.

Exact source/provider/binary identities and raw logs are outside the source tree
under the existing `completion-execution-01/packed-condition-*` records. The
six reviewed contracts contain24 triangle/layout modes. Mapping/evidence updates
preserve Native20 and historical execution outcomes and separately review all
ten additive integration-artifact bindings. Final Doxygen and consistency
results are recorded in that same evidence root.

The numerical limitation remains unresolved. Record it and continue six packed
SPRFS/HPRFS refinement routes; exact source/emission review and both1,296-case
native prerequisites are ready. Existing inverse, solve and producer limitations
remain open. No provider, fallback, XBLAS, platform, security, notice or release
decision follows from this engineering checkpoint. The full programme remains
incomplete.
