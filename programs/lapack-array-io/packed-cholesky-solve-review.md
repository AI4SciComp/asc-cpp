# Reusable ordinary packed Cholesky solves

The eleven audited candidate06 files implement actual S/D/C/Z PPTRS with
explicit optional-provider overloads and metadata-only workspace queries.
[The frozen admission](packed-cholesky-solve-admission-review.md) binds the
selected TPSV source arithmetic, full emitted GNU signatures, exact provider
archives and ten-object closures per ABI. The helper rejects nonempty native
N*(N+1) or final NRHS+1 overflow before narrowing. Empty order or RHS remains
local in the ASC dimension domain. No RFP/dense conversion or native credit
is inferred. All 32 ordinary packed positive-definite routines remain required.

Factors are raw borrowed triangular data, unchanged across multiple RHS and
repeated solves. Complex diagonal imaginary parts are significant. Queries
read metadata only; p row-factor and N*NRHS row-RHS conversions use exact
queried live caller objects. Preflight precedes numerical mutation and provider
entry. Full-width INFO starts at MIN: nonzero, missing and partially written
values report a defect; row publication is withheld and direct writes survive.
Zero factors follow native TPSV arithmetic with INFO0, without certifying a
finite or useful solution. There is no factor provenance or finiteness scan.

## Executed candidate evidence

Audit `8dc6f75c0577f93e14f925c90087b36c28e02204bf07fe0d69ad3e149f4a8b25`
and its completion seal bind 142 original records, including four original
strict failures. Candidate02 removes an unused public-test include and adds
a nodiscard annotation; candidate05 removes an unused memory-test include.
All numerical assertions are preserved. Six final configurations each pass
6/6 tests with zero skips. They freshly compile 66 adapter/test/support objects
and explicitly reuse/reverify the earlier 65 primary TUs per lane; this is
not fresh root compilation. Foreign archives remain unsanitized. Sixteen
exact-input strict checks, four isolated normal/no-exception header checks
and the current eleven-file formatting check pass.

Each lane passes 10,080 public workflows/177,745 checks: four scalar types,
N0/1/2/3/5, RHS0/1/3, both triangles and independent A/B layouts, seven dyadic
scales, repeated RHS/factor reuse, real and nonreal complex diagonals. The
independent widened infinity-norm equation uses no additive-one denominator;
known solutions also bound forward error. Factor bytes, padding and packing
guards are preserved. The 2,752 failure profiles/13,985 checks cover actual
A/B overlap, metadata/workspace/provenance/placement errors, exact native
arguments, INFO omission/partial writes and scoped C++/libc allocations.

Linux protected containing arrays prove 192 metadata-only queries and 128
local empty executions read/write neither complete numeric operand. All
arrays have real C++20 containing lifetimes and full valid backing. The 160
scalar raw-factor profiles include zero diagonals with zero/nonzero RHS and
negative-real/pure-imaginary factors, checking exact |d|^2 equations for the
finite cases. Pure source admission passes 37,058 checks; four complete
private signatures per ABI include the hidden CHARACTER length.

## Root registration and remaining acceptance

PPTRS4 becomes in_progress, bringing required Reference rows to 330 partial
and 1,783 not_started; none is fully verified. Native20 remain separate
implemented_unverified. The executed root integration is recorded below. Full routine/mode, concurrency, platform and final
normalized acceptance remain required. Other packed families, all remaining
P00-P11, 70 prior ordinary mathematical failures and 130 missing required
XBLAS routines remain visible. The earlier admission13-plus-seal14 records
are already normalized and are not counted again in this import.


## Executed v22 integration

Audit `7642ea72805f6dfda01215b5bcc5b3c9a2c80355cffa14658821200712bffe74`
and its seal bind the 1075-file frozen product and 30 original records. Both
GNU Release suites execute 896 tests: 826 pass and 70 required mathematical
tests fail. Affected Debug passes 76/84 and ASC-only Clang19 sanitizer passes
23/31 per ABI, each retaining eight mathematical failures. Zero tests skip;
there are no sanitizer diagnostics. All 67 Core/Dense/provider translation
units compile freshly in six lanes, producing 402 primary objects. Foreign
archives are unsanitized. Four relocated packages pass all 26 family consumers,
including PPTRS's 10,080 workflows/177,745 checks. Six static checks pass;
Doxygen covers 121 headers and 2,164 public members with zero warnings.
Candidate strict/header checks bind unchanged source/compiler inputs.

Provider-free audit
`49701e213e17ca70e4a43552d462848e00a2a276a8e03cde7ef70d8fd3bc6fbe`
and its seal bind fresh GNU static/shared scoped 16/16 checks each, 60 primary
objects and 12 test/support objects. Both optional packed-factor/solve headers
are absent; there is no Fortran discovery or linkage. This is scoped baseline
evidence, not full baseline or shared-provider verification. These 40 new
raw/completion records bring the normalized ledger to 3,514. The separately
audited PPTRI4 candidate remains unregistered, with its 87 records already
normalized. Complete routine/mode/concurrency/platform acceptance, all P00-P11,
70 ordinary mathematical failures and 130 missing required XBLAS stay open.


The three final documentation changes pass six fresh checks. Audit
`e687993c7ebaecc3f52ae2b8607ebbd0d07a74acd01acc468399640a4c8f518c`
rehashes 9,790 unchanged numerical/default compiler inputs and artifacts,
including installed consumers. Its frozen source archive is
`9d3672e3e12e2327ef3596e154112948245ed9553d5248f72eb7614c690075f1`.
Numerical executions retain their original v22 identities; no numerical rerun
on changed prose is claimed. The ledger contains 3,520 normalized records.
