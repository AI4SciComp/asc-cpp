# Ordinary packed Cholesky inverse candidate

The ten files in unregistered candidate04 implement actual S/D/C/Z PPTRI,
metadata-only packing queries and source-specific integer admission. The
[frozen admission](packed-cholesky-inverse-admission-review.md) defines raw
factor, real complex-diagonal, selected inverse, singularity and publication
semantics. Existing ordinary packed descriptors and explicit caller conversion
preserve module isolation and the optional pinned provider boundary. Native20
and the other 28 packed positive-definite routines remain separate requirements.

Audit `70d827b8ecaba264b823f57d7916b045e419fdb58b4ed59ab93498a06a993008`
and its completion seal bind 86 original records and 60 fresh final objects.
Six final configurations pass 5/5 tests each, zero skips; 65 prior primary
ASC TUs per lane are explicitly reused and rehashed. Foreign archives remain
unsanitized. Fourteen exact-input strict checks, four isolated normal and
no-exception public-header checks and current ten-file formatting pass.
Initial metadata incorrectly said three compiled objects; actual initial
compile databases have four. The expanded final candidate has ten per lane.
The four original strict failures remain recorded. Candidate02 removes one
unused adapter include and separates five verifier declarations; every
mathematical expression and assertion remains unchanged.

Each lane passes 1,344 independent public profiles/85,457 checks. The fixture
L=D*(I+q*S) uses a lower shift with S^N=0; its independently known inverse is
the finite geometric series times D inverse. Expected A inverse is formed
from that series, without calling a provider factor/solve/inverse routine.
Both A*X and X*A identity products and the known inverse are checked with
widened infinity norms and epsilon/order-based bounds, without an arbitrary
additive-one denominator. All four scalar types, both triangles/layouts,
N0/1/2/3/5, seven even dyadic scales and first/last zero diagonals are covered.
Successful selected diagonals are positive/real; source-defined singular
returns preserve the complete factor. Physical slots and all guards are
independently enumerated and checked.

Each lane also passes 384 failure/allocation profiles/3,312 checks. Exact
native order, triangle and hidden CHARACTER length are observed on real and
injected calls. Full-width missing/negative/impossible/partially written INFO
retains correct diagnostics. Injected defects write a live native destination:
column writes survive and row publication is withheld. Injected positive INFO
preserves input according to the source-defined singular branch; real singular
mathematics is separately exercised above. Metadata, workspace/operand aliases,
placement and stale identities reject before numerical mutation or native
entry. Initial/repeated real calls and queries observe no C++ or wrapped
static-link libc allocations within the recorded scope.

Linux protected containing arrays prove 64 metadata-only queries, 16 local
empty executions and 16 empty stale-plan rejections access no numeric object
or guard, with zero observed C++ allocation. Actual C++20 containing-array
lifetimes and full backing are established before mprotect. These observations
do not establish another platform or foreign-sanitizer coverage. Each lane
also executes the 4,918-check source helper and four complete private signatures.

The 87 candidate/completion records bring the normalized ledger to 3,474.
The earlier admission26 records are already normalized and not counted again.
PPTRI4 is still unregistered; the released mapping remains 330 Reference
in_progress/1,783 not_started, native20 implemented_unverified and zero fully
verified. Complete current PPTRS v22 evidence/documentation and commit it,
then import these exact ten files and run fresh root/installed verification.
All routine/mode/concurrency/platform closure, P00-P11, 70 prior mathematical
failures and130 missing required XBLAS entries remain required.


## Root registration

The ten candidate04 files are imported with atomic optional header/source,
profile v23, installed-family and contract registrations. A single stale
signature-test comment now says PPTRI instead of PPTRF; the complete native
signature declarations/assertions are unchanged. Both original and imported
hashes are recorded. Fresh root tests and exact-file strict checks are recorded below.
The candidate's 87 records were already normalized and are not counted again.

PPTRI4 is now in_progress: 334 Reference partial/1,779 not_started, native20
implemented_unverified and zero fully verified. Executed 68-primary-TU, 122-header
and 27-family verification is recorded below. All remaining routines, full normalized
mode/concurrency/platform acceptance, P00-P11, 70 prior mathematical failures
and 130 missing required XBLAS entries remain required.


## Executed v23 integration

Audit `98cbc9f3d9dc791841fce1c437942568e5a9bead430db6e0202921a029211e00`
and its seal bind the 1085-file frozen product and 34 original records. Both
GNU Release suites execute 903 tests: 833 pass and 70 required mathematical
tests fail. Affected Debug passes 83/91 and ASC-only Clang19 sanitizer passes
28/36 per ABI, each retaining eight mathematical failures. Zero tests skip;
there are no sanitizer diagnostics. All 68 Core/Dense/provider translation
units compile freshly in six lanes, producing 408 primary objects. Foreign
archives are unsanitized. Four relocated packages pass all 27 family consumers,
including PPTRI's 1,344 workflows/85,457 checks. Six static checks pass;
Doxygen covers 122 headers and 2,172 public members with zero warnings.
Four fresh strict checks bind the corrected signature comment and current public header filter in both ABIs. Other candidate checks bind unchanged inputs.

Provider-free audit
`fcf751f37d68d99a3f171bf11ab62a5a831a037489d5bf1f7e16c4c19fb9686f`
and its seal bind fresh GNU static/shared scoped 16/16 checks each, 60 primary
objects and 12 test/support objects. All three optional packed Cholesky headers
are absent; there is no Fortran discovery or linkage. This is scoped baseline
evidence, not full baseline or shared-provider verification. These 44 new
raw/completion records bring the normalized ledger to 3,688. The separately audited PPSV4 candidate remains unregistered, with its 91 records already normalized. PPEQU admission14 records are normalized; typed work remains. Complete routine/mode/concurrency/platform acceptance, all P00-P11,
70 ordinary mathematical failures and 130 missing required XBLAS stay open.


The three final documentation changes pass six fresh checks. Audit
`d10a7cadabaa99fcbbfa22fe68eb874499c4e575810e394bb1ce3a7b57299fd4`
rehashes 10,050 unchanged numerical/default compiler inputs and artifacts,
including installed consumers. Its frozen source archive is
`4208cf11376abecc6ee0ed901777518842401c0f6b26a7e3d1e8dcd75df5c917`.
Numerical executions retain their original v23 identities; no numerical rerun
on changed prose is claimed. The ledger contains 3,694 normalized records.
