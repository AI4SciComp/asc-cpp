# Ordinary packed positive-definite refinement candidate

Five unregistered files in external candidate02 implement the optional
QueryPprfsWorkspace/Pprfs declarations and adapter, a private source-count
helper, its literal test and four complete private signature checks. The
frozen direction and source admission are in the adjacent admission review.
The adapter compiles for all four scalars with independent packed AP/AFP
and full B/X layouts, explicit workspace, preflight-before-write ordering,
local empty completion, signed INFO sentinel and retained error diagnostics.
Original Hermitian AP packing reads only the real diagonal, while factor AFP
packing reads all coefficients. Runtime behavior is not yet tested or accepted.

Candidate01 fails both GNU11 Debug builds on copying a full matrix descriptor
in a range loop under -Werror. Its four configure/build records and eleven
passing strict/header/format records remain unchanged. Candidate02 changes
only two loops to const references. Both Debug ABIs compile three fresh
objects and pass two tests: 25,310 source-count checks and four full signatures.
Eleven fresh strict/header/format checks pass. These tests do not call Pprfs;
the public adapter is only compiled into a static archive. No numerical,
failure, memory, allocation, installed-family or full-mode credit is assigned.

Compile audit
`8a58988dbe335055de658a9f4c59c26de8c68e34a629bee18dd7122193c5310b`
and its seal bind 34 raw records plus the seal. Actual candidate sources each
have 1110 files: frozen v25's 1105-file base plus five owned files. Metadata
identifies the inherited base archive; explicit source maps cover additions.
Six objects are fresh across two ABIs; the exact v20 65-primary-TU libraries
are rehashed and reused. The seal compares the adapter archive's sole member
with its fresh object. The ledger now contains 4,052 normalized records.

Next add independent refinement and residual/error-estimate workflows,
multiple RHS/reuse/scaling/wrong-triangle/local paths, required extreme math
gates, full failure/preflight/INFO and protected-memory/allocation observation,
then execute and audit fresh candidates before root import. All 2113 required
routines, 130 missing XBLAS entries and the complete P00-P11 acceptance remain
open. This is root self-review; no independent approval is asserted.


## Executed numerical slice: candidate06

The seven unregistered files now include an independent public workflow and
four ordinary required scalar mathematical gates. Each of six configurations
(GNU11 Debug/Release and ASC-only Clang19 ASan/UBSan, both actual ABIs) runs
seven tests: three pass, four mathematical gates fail, and none skip. Each
public run covers 26,880 profiles: orders 0/1/2/3/5, RHS counts 0/1/3, both
triangles, sixteen independent AP/AFP/B/X layouts, seven scales and two calls.
Independent residual, forward-error and fixture-specific error-estimate checks
pass. Input preservation, output padding and workspace guards are checked.
LP64 records 9,042,432 checks and ILP64 8,963,584; the 78,848 difference is
exactly the additional bytes occupied by live ILP64 INTEGER workspace.

For each scalar, twelve of 168 required extreme checks fail. The two smallest
subnormals and maximum finite scalar A=B, X=1 yield native FERR infinity with
INFO0, while the independent widened formula is finite. Direct pinned calls
reproduce the failure in all six configurations. Unit, minimum-normal and
maximum-quarter controls pass. BERR agrees with its safeguarded formula.
Unscaled inverse intermediates overflow before weighting for the subnormals;
the native denominator overflows for maximum finite input. These remain
ordinary failures, with no source patch, rescaling, fallback or waiver.

Candidate03's two public strict failures (length and nested conditionals) and
candidate05's LP64 build/strict narrowing failures remain recorded. Candidate04
refactors helpers without changing thresholds. Candidate06 separates native
INFO from numeric comparison values. Its six CTest exit-8 records remain
failures; successful evidence auditing does not turn those tests into passes.

Numeric audit
`6aef05bf3fb12646ad1547ba006ceea87ef8641a264a97c781d57293ba98693f`
and its passing seal bind 94 new raw records plus the seal, including eleven
nonzero records. The ledger contains 4,193 normalized records. The earlier
35 compile-only records are retained without duplication. Thirty objects are
fresh in the current six lanes; exact v20 libraries with 65 primary ASC TUs
per lane are explicitly reused. Foreign archives are unsanitized. Ten current
strict checks, four isolated headers and seven-file formatting pass through
explicit source identity binding. There is no public Clang, shared-provider,
cross-platform, installed-family or complete mode credit.

Next complete failure/preflight, protected-memory and allocation observation
in a fresh candidate. The two drafted interception files have not yet been
compiled or executed. All four PPRFS rows remain unregistered and incomplete;
all 2,113 required routines and 130 missing XBLAS routines remain required.
This is root self-review, not independent approval.


## Executed fault slice: candidate09

The three interception/driver files now execute in all six configurations.
Each run passes 6,272 fault profiles and 110,464 checks: all sixteen layouts,
both triangles, four scalars, two real calls, thirteen injected outcomes and
34 preflight cases. Full-width, missing and partial INFO, FERR/BERR retention,
negative and nonfinite estimates, row-X publication, report provenance,
shape/placement/stale plans, all six operand-to-workspace aliases and protected
metadata aliases are checked. Query and real/injected execution show zero
observed C++ or wrapped static-libc allocations in their measured scopes.

Each configuration executes eight tests: four pass and the same four ordinary
mathematical gates fail, with no skips or sanitizer diagnostics. Candidate07's
four strict failures (including its missing test include path) and candidate08's
two remaining cast strict failures are preserved. Candidate09's four strict
checks and ten-file formatting pass. Production and all seven prior numerical
sources are unchanged from candidate06.

Audit `9484966a038a6294890c8dbaac2c54a19701db91677d81abdbe7e8353220a6fd`
and its seal bind 47 raw records plus the seal, including fourteen nonzero
records. The ledger now has 4,241 normalized records. There are seven owned
and two support objects per current lane, 54 fresh objects total. Candidate07
and candidate08 each compile nine objects; their copied metadata incorrectly
said eight owned objects. Actual compile commands prove seven owned objects,
and the audit gives no extra credit. Exact prior ASC libraries and unsanitized
foreign archives remain explicitly reused. This is root self-review.

Mutual operand alias classes, ignored complex diagonal access, protected-memory
completion, concurrency, installed-family, platform and full mode acceptance
remain open. Candidate10's memory tests are in progress. No required routine,
mathematical failure or missing XBLAS dependency is removed from scope.


## Executed memory and mutual-alias slice: candidate11

Six configurations each execute nine tests: five pass, four unchanged ordinary
mathematical gates fail, none skip, and no sanitizer diagnostics occur. Each
failure run passes 10,112 profiles and 137,344 checks. The new 3,840 profiles
cover all fifteen operand pairs with exact and partial overlap. Arrays have
valid backing; mixed complex/real aliases use C++'s interleaved component
access guarantee. Query and execution reject before numerical writes or
foreign entry, reset ordinary dirty reports, and show zero observed allocation.

Each memory run passes 1,600 profiles: 768 fully protected queries, 512 local
calls without reading AP/AFP/B/X or optional scratch, 768 stale-plan rejections,
768 native boundary calls and 64 ignored complex diagonal calls. Full B/X
row or column padding occupies protected pages; packed AP/AFP and error
vectors meet protected boundaries at either end. A real one-element complex
array straddles a page boundary with only AP's imaginary component protected.
Both triangles and sixteen layouts complete without reading that component.
Placement array new establishes actual containing-array lifetimes throughout.
Original bytes and restored guards remain checked. These are Linux observations;
foreign libraries remain unsanitized and platform closure is not claimed.

Audit `a4975972c983d7f921194eb9fcf6a65f41283cafc25c1cba09ec4ee3d391f7a4`
and its seal bind 56 raw records plus the seal, bringing the ledger to 4,298.
Twelve CTest exit-8 records remain failures. Both candidate10 and candidate11
compile eleven objects per lane, 66 each: eight owned and three test/support
objects. Candidate10's copied scope first says eight tests before stating nine;
executed registrations and JUnit establish nine. No extra credit is assigned.
Four current strict checks and eleven-file formatting pass. The production
adapter remains unchanged from candidate06; exact older strict and numerical
evidence is bound by its source identity. This is root self-review.

Candidate12 adds further shape/stride and packing preflight cases. Complete
its fresh configurations and audit before root import. Raw-factor and wrong-
triangle provenance, complete routine/mode/concurrency/platform closure and
installed-family verification remain open. All 2,113 routines, the four PPRFS
mathematical gates and 130 missing required XBLAS routines remain required.


## Executed preflight extension: candidate13

Nine additional rejection classes add 1,136 profiles. Seven shape/stride
cases cover all 128 scalar/triangle/layout combinations: B/X column mismatch,
FERR/BERR length mismatch, BERR increment and stale original B/X leading
strides. Short and misaligned packing each cover the 120 profiles that require
packing. All earlier assertions remain. Each of six configurations passes
11,248 failure profiles and 144,160 checks, plus 1,600 protected-memory
profiles and the independent public/count/signature tests. Nine CTests execute
per configuration: five pass and the same four required math gates fail,
with zero skips or sanitizer diagnostics.

Candidate12 passes its Debug LP64 behavioral tests but fails both strict checks
because ConfigureRejection has 88 lines against the accepted 80-line limit.
Candidate13 extracts the packing helper without changing assertions. Both
current strict checks and eleven-file formatting pass. All other exact-input
strict/header evidence is bound through candidate11's audit chain.

Audit `7fc6ceced71dc199913af4457a3dfaa97e58c7f3f8d8bdc15385af9a62597ce6`
and its seal bind 34 raw records plus the seal, including nine nonzero records.
The ledger now contains 4,333 records. Candidate13 compiles 66 fresh objects
across six lanes; candidate12 compiles eleven historical objects. Exact prior
ASC libraries are explicitly reused and foreign archives remain unsanitized.
Production is unchanged from candidate06. This is root self-review, with no
complete routine/mode/platform or independent approval claim.

Candidate13 is the preserved current eleven-file draft. Review remaining
raw-factor/wrong-triangle provenance obligations, then reconcile its frozen
v25 base with committed PPCON v26 before root import. Root and installed-family
verification must use that new identity. All four PPRFS mathematical failures,
all 2,113 required routines, 130 missing XBLAS and remaining P00-P11 stay open.
