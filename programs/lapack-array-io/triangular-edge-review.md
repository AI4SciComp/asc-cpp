# Triangular additive edge acceptance

Six current tests pass, zero failures/skips:7952 profiles/62224 assertions perlane. 24 fresh test objects;60 prior production TUs perlane rehashed and reused. No new production build, foreign instrumentation, installed package or full-suite execution. 256 additive edge profiles preserve all prior7696 profiles byte-for-byte. Existing required mathematical/XBLAS and wider acceptance gates remain open. The new cases are64 real zero-diagonal TRTI2 source-fidelity profiles and192 live A/B-overlap query/execution rejections; neither implies mathematical invertibility for singular input. Fresh strict bothABIs, one-file format and coverage validation pass. Reference286partial/1827not_started of2113 and20native implemented_unverified remain unchanged.

Frozen tree `78995b4b105dfd99f5691643dae615b48c6cdf59`, archive SHA256 `877fad72c5c9f18b1c1b917352433984bf10f0662a748978091d01fb6f2bf51e`. Completion audit `p05-triangular-edge-acceptance-01/audit.json`, SHA256 `9a222d37e10396f521e826c3d7af771618dbc5182aa5ca5cae4737b83e33d55a`.

Only triangular_fault_test.cc and its12 coverage mapping artifacts/narratives
change the exported product. Removing the new cmath include, two test
functions and two call blocks reproduces the previous test byte-for-byte.
All prior assertions,7696 profiles and61072 checks survive. New cases add256
profiles and1152 checks; the current test executes7952 profiles/62224 checks.

TRTI2 zero diagonals reach the real pinned routine for each scalar, triangle,
layout and both zero-diagonal positions. Native INFO remains0 with nonfinite
reciprocal output; no fabricated singular diagnosis, finite result or inverse
validity claim is made. Ignored triangle, padding, RHS and workspace red zones
remain unchanged. These are source-fidelity checks on singular input.

The overlapping solve tests use the same real live scalar backing for A/B,
with independent layouts, upper/lower, unit/nonunit and N/T/C. Both formula
query and execution reject the overlap before any of12 native routes runs;
every numeric/workspace byte survives and a stale report is reset exactly.

Six fresh four-TU test builds execute against previously audited, rehashed
60-TU Release/Debug/ASC-only sanitizer production libraries.24 fresh test
objects, binaries, link commands, actual compiler dependencies and runtime
closures are bound. Foreign archives are unsanitized. Two earlier Release
overlays and their strict checks are retained as separate evidence; their
passes are not counted again as the six current lanes.

No new installed package or full-suite execution is claimed because all
production code, public headers, CMake and installed consumers are unchanged.
Full normalized mode contracts, deeper read/allocation observation, extremes,
platform/shared-provider gates and the remaining required families stay open.
