# Packed triangular admission checkpoint

Pinned S/D/C/Z TPTRI/TPTRS/TPCON/TPRFS are16 actual required public
computational routines. All16 remain not_started in release coverage; this
source and pure-count work assigns no callable or verified capability credit.
The next implementation slice is the8 TPTRI/TPTRS inverse/solve routes.

External `p05-packed-triangular-source-01/inventory.json` retains all16 full
inventory rows and exact source hashes. Both actual provider archives contain
the16 entry symbols. Conservative closures contain91 objects perABI, no
writable static symbols, and external cabs/cabsf/memcpy/memset after recording
the preflight-excluded XERBLA diagnostic edge. This mechanical closure is not
a complete source/progress/allocation proof.

32 actual GNU compiler-emitted source signatures match the installed typed
declarations, including native primitive integers and trailing character
lengths. The two emitted-compare records pass. Complete fixed-format statement
comparisons cover14 precision pairs: TPTRI/TPTRS/TPCON/TPRFS/LACN2/LANTP/LATPS.
The first comparison preserves its failure on explicit REAL(N) versus implicit
assignment conversion in LANTP's Frobenius branch. A second script explicitly
normalizes that reviewed conversion; all14 pairs match without erasing branch
labels or reordering executable statements.

`p05-packed-triangular-counts-01` freezes the first private integer helper and
independent cursor oracle. GNU Release CTest passes1/1 with1465114 checks and
zero failures/skips; strict Clang-Tidy18 passes. The oracle walks selected
source expressions using small virtual native ranges and separate actual
LP64/ILP64 boundaries; it fabricates no matrix backing span. Upper inverse and
forward solves protect packed cursor p+1; descending paths protect the actual
predivision n*(n+1). Lower nonunit TPTRS also protects JC+n before subtraction.
Zero-RHS unit solves preserve the no-numeric-read branch. ASC descriptor
construction has its own separate checked n*(n+1) storage bound.

`p05-packed-triangular-draft-01` preserves that helper/test plus an uncompiled
packing helper and inverse/solve adapter body. Packed layout conversion uses
only explicit caller packed workspace; it never densifies A. Unit diagonal
slots remain unread/unwritten. Zero-RHS nonunit row-packed calls convert only
diagonal values for the source's singularity scan. The draft has no public
declarations, complete adapter TU or public/fault tests yet. None is installed
or registered. Finish source/control review and typed implementation/tests
after the current v16 documentation checkpoint; preserve these artifacts.
