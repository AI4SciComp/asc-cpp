# Ordinary packed positive-definite expert candidate

The five preserved candidate01 files implement24 optional Ppsvx declarations
and their FACT N/E/F adapters, the private integer helper, its literal test
and complete private native signature checks. The adjacent admission review
contains the bound source proof and frozen typed direction. This code is
external and unregistered; no typed runtime behavior is accepted yet.

Both GNU11 Debug ABIs compile three fresh objects and pass two tests:25,331
source-count checks and four complete21-argument signatures. Eleven strict,
isolated-header/no-exceptions and formatting commands pass. The public
adapter is compiled into a static archive but neither test calls Ppsvx.
No numerical, failure, memory, allocation or installed-family credit follows.

Compile audit
`5046909718dc0e0ddd47f6e408df1e86ce6b176cc376f313feb5cc1f1f4206f4`
and its passing seal bind nineteen passing raw records and six fresh objects.
The sole adapter archive member is compared with its fresh object. Candidate
source has1132 files: exact v27 base1127 plus five owned files. Inherited
metadata describes the base archive/tree; actual source maps cover additions.
Audited v26's71 primary ASC TUs are explicitly reused and rehashed. Foreign
archives remain unsanitized. Twenty records bring the normalized ledger to4481.

Candidate02 adds a public consumer with independent residual/forward/error
checks, factor reconstruction, scalar condition-one examples, all layouts,
equilibration and reuse of actual returned factors. It is executing; its
results are not yet accepted. Required extreme mathematical gates, injected
and real failures, protected memory/alias/allocation, concurrent calls,
installed clients, complete modes/platforms and all remaining P00-P11 work
remain open. All2113 routines and130 missing required XBLAS remain required.
This is root self-review, not independent approval or full verification.


## Executed numerical candidate03

Audit `8f1a08632627cf1aaec25f53d4330041c186645b9326b3c7f5d260d8437fe387`
and its seal bind48 new raw records plus the seal. Six configurations each
pass3/3 tests, with no skips or ASC sanitizer diagnostics. Each public run
passes65,280 profiles and31,119,488 LP64 or30,832,256 ILP64 checks. The287,232
difference is precisely the four extra native INTEGER bytes per active order
for real scalars:2*32 layouts*3 RHS values*sum(0,1,2,3,5)*34 calls*4 bytes.
Source counts pass25,331 checks and all four complete signatures pass.

Explicit L with diagonal2..N+1 and quarter-valued offdiagonals defines
A=L*L^H. Independently formed B=A*known_X tests the original unscaled system.
All sixteen AP/AFP/B/X layouts, both triangles, four scalars, N0/1/2/3/5,
NRHS0/1/3 and FACT N/E/F (both supplied scaling choices) execute. Five base
exponents include half-range tiny/huge values. FACT E additionally covers
near-normal-minimum and near-maximum inputs requiring actual equilibration.
The tests reuse actual newly returned factors with original B restored.

Independent factor reconstruction uses the actual scaled factor equation.
Solve residuals, forward errors and finite error estimates retain the PPRFS
fixture's unchanged dimension-scaled bounds, including zero-denominator and
BERR=1 zero-solution cases. Scalar condition-one checks, scaling decisions,
AP/AFP/B/S mutation, query preservation, workspace pair units and all storage
guards pass. No universal FERR certification is asserted. Multi-entry analytic
condition examples and required extreme scalar mathematical gates are next.

Candidate02 passes its two Debug numerical suites but both public strict
checks reject three combined variable declarations. Candidate03 separates
only those declarations, preserving every assertion and production byte.
Two current public strict checks and six-file formatting pass; six unchanged
other strict checks and four isolated-header checks are bound to candidate02.
Twenty-four current objects and eight historical objects are fresh. Exact
v26's71 primary ASC TUs are rehashed and reused, with foreign code unsanitized.
The ledger now contains4530 records. Complete failure/preflight, protected
read/write/alias, allocation, concurrency, installed and platform/mode gates
remain open. PPSVX4 is still unregistered; no fully verified routine is added.


## Executed diagonal and extreme candidate06

Audit `da1ffd0cd841c017f69ed16a98ba3bbd9820fba6fb3c6c0a3e9d5c810437d0c0`
and its seal bind65 new raw records, including15 nonzero records. Six current
configurations execute seven tests: three behavioral tests pass and four
ordinary required scalar mathematical tests fail, with zero skips or ASC
sanitizer diagnostics. Public tests pass82176 profiles and39097472 LP64 or
38720128 ILP64 checks. The extra16896 diagonal/reuse profiles cover analytic
min(diagonal)/max(diagonal), including actual scaling. The377344 check delta
is the previous287232 plus90112 literal INTEGER guard-byte checks. Counts
pass25331 checks; four complete signatures pass. Thirty current objects and
35 historical objects are fresh; exact v26 ASC libraries remain explicitly
reused and foreign archives remain unsanitized.

Candidate04 fails7040 public assertions. GDB identifies a diagonal N2 RHS
with zero residual and nonzero normwise denominator: one equation is zero,
so the componentwise SAFE1 floor makes BERR exactly one. Candidate05 derives
zero component denominators independently from fixture A, B and known X,
and requires BERR exactly one there. All other equations retain the original
small bound; residual/forward/FERR/condition assertions and tolerances are
unchanged. Its six runtime lanes pass the public test, while two strict
checks require nodiscard on the new predicate. Candidate06 adds only that
attribute; current public strict2/format pass, and unchanged math strict and
prior header/adapter checks are bound. All historical failures are retained.
The first auditor incorrectly demanded JUnit status run for failing tests;
audit02 requires fail exactly for failed cases and run for passing cases,
while retaining the failed auditor/log. It does not change CTest results.

Each scalar gate has600 checks and44 mathematical failures. Four independent
layout/triangle combinations fail11 cases each: FACT N and both supplied F
choices at the two smallest subnormals and maximum finite value, plus FACT E
at the two subnormals. Scalar A=B is positive finite, with condition one and
X=1. A widened finite error bound accounts for roundoff and the safe floor.
All remaining mapping, guard and rounded-factor checks pass; direct pinned
calls reproduce every failure. FACT E additionally overflows S*S before
multiplying subnormal AP, consistent with the reviewed source order. No
provider patch, rescaling, fallback or tolerance waiver is introduced.

The ledger now contains4633 normalized records. PPSVX4 remains external and
unregistered; failure/preflight, protected-memory/allocation, concurrency,
installed, full mode/platform and remaining P00-P11 acceptance stay open.
All2113 routines and130 missing required XBLAS remain required. Local PPRFS
commit87cd646 matches all1127 audited product files; its78 required root
mathematical failures remain visible. No fully verified routine is added.


## Executed native fault candidate08

Audit `db24cdcb44aa23cece80fe1db23a4c3f781d4fb03f7410c22f57e5c204f4b71d`
and its seal bind58 raw records plus the seal. Six configurations each run
eight tests: four behavioral tests pass, four ordinary scalar mathematical
gates fail. No skips or sanitizer diagnostics occur. Each fault run passes
13312 profiles and418816 checks: four scalars,32 triangle/layout choices,
four FACT modes and26 real-control/synthetic cases. Actual N3 and NRHS2
with padding distinguish row/column packed ordering and native leading strides.

Synthetic cases include missing, negative, excess and half-width INFO;
first/last/invalid supplied factor failure; missing, negative, NaN and infinite
RCOND/FERR/BERR; warning INFO and malformed/changed EQUED. The tests check
all21 arguments, including all three hidden character lengths and incoming
full-width INFO sentinel; exact call counts, report category/validity/index,
factor authorization, direct writes retained and packed AP/AFP/B/X withheld
on defects, raw scale/condition writes, NaN error staging and complete operand
padding/guards. Partial row AFP preserves old imaginary diagonals. Real
provider controls independently give X columns1/2 and condition1/4. Fake
numerical values count only toward adapter behavior. No global handler changes.

Candidate07 passes runtime but four strict checks reject constant naming and
nested test conditionals. Candidate08 changes only those two test TUs; all
current four strict checks and formatting pass. Forty-two current and42
historical objects are fresh. Exact v26 ASC libraries are reused and foreign
archives remain unsanitized. Link/nm/ldd records bind all four real and wrapped
entrypoints. The public82176 profiles,25331 source counts and four signatures
continue to pass; each required scalar mathematical gate still fails44/600.

Two preserved auditor failures concern the mechanical style proof: stdin
formatting lacked the filename needed to keep the matching header first,
and renaming the aligned constant adds one indentation column. Audit03
supplies the filename and compares unchanged lines after removing leading
indentation. Report/argument expectation refactors were reviewed separately;
all assertions and observed test counts remain. No test gate is weakened.
These59 records bring the ledger to4692. Candidate09 adds real failure and
local supplied-coefficient tests; its execution/audit and all remaining
preflight, protected-memory/allocation, concurrency, installed and full modes
remain open. PPSVX stays external/unregistered and all2113 routines/130 missing
required XBLAS and P00-P11 acceptance remain required.


## Executed real failure candidate09

Audit `ce0d44fa54c0782b1f5034bec01715a86cf2b254fe879a5b2db69010b2658885`
and its seal bind27 raw records plus the seal. All six configurations execute
eight tests: four behavioral passes and four ordinary required scalar math
failures, with zero skips or sanitizer diagnostics. Each fault/real run passes
19712 profiles and479488 checks. The added6400 profiles contribute60672 checks:
2816 local rejections*7,2048 unused/output-scale controls*11 and1536 real
non-positive cases*12. All previous fault and numerical assertions remain.

FACT N/E calls with a zero or negative first/middle/last diagonal report exact
native pivot1/2/3, RCOND0 and documented partial AFP; AP/B/X/FERR/BERR remain
byte-identical. The expected partial factor is derived from the diagonal
matrix, with computed zeros compared numerically and untouched storage checked
byte-for-byte. FACT E's failed PPEQU writes its raw diagonal values into S,
without equilibration. Supplied exact-zero AFP is rejected locally at each
position with absent INFO and unchanged operands/scratch. FACT F with scaling
rejects zero, negative, Inf and NaN S at both ends before inspecting a zero
factor. FACT F without scaling and FACT E output-only S accept those initial
values; real calls independently solve X columns1/2 with condition1/4.
Metadata-only queries remain valid and preserve all bad coefficient bytes.

All four scalars,32 triangle/layout choices and applicable FACT variants run.
Forty-two objects are fresh; exact v26 ASC libraries remain reused and foreign
archives unsanitized. Both current strict checks and formatting pass. Public
82176 profiles,25331 counts and four signatures still pass. Each scalar
mathematical gate remains an ordinary44/600 failure. These28 records bring
the ledger to4720. Candidate10 adds metadata/workspace/alias rejection tests;
scalar-output aliases, remaining modes/placement, protected-memory/allocation,
concurrency and installed/root/full-program acceptance remain open. All2113
routines and130 missing required XBLAS remain required; no fully verified credit.


## Executed preflight candidates10–11

Candidate11 audit `f6fa0e255b94555f5347ac8b59b8c64d8c8b1613608991d6eeb031e80dec99e1`
and its seal bind54 raw records plus the seal. All six current configurations
execute8 tests:4 behavioral passes and4 ordinary scalar mathematical failures,
with zero skips or sanitizer diagnostics. Each fault/real/preflight run passes
67512 profiles and720536 checks. The added47800 profiles contribute241048
checks:47800*5 plus2048 complete literal identity oracles. Tested rejections
cover provider/scalar/routine/order identity, triangle/four layouts/FACT mode,
original B/X strides, five plan fields, shape/vector metadata, invalid triangle,
required workspace capacity/alignment/Device, metadata and live-operand scratch
aliases, unused-region overlap and all exact/partial matrix/vector pairs.

AP/AFP containing arrays grow8 to24 entries so matrix aliases have real backing;
prior active fixtures/assertions remain and all added padding is checked.
Preflight checks preserve operands/scratch, prevent native entry, and reset the
report except when report metadata itself aliases scratch. Candidate10's two
strict failures remain recorded. Candidate11 replaces a bounded integer selector
with an exhaustive uint8_t enum, preserving all five cases. Current two strict
checks and formatting pass.42 current fresh objects and42 historical objects
are bound; exact v26 ASC libraries are reused and foreign code is unsanitized.
Public82176 profiles,25331 count checks and four signatures pass unchanged.
Each required scalar mathematical test still fails44/600 checks.

These55 records bring the ledger to4775. Candidate12 handles scalar outputs,
placement and supplied flags; empty/NRHS0/raw-factor, protected-memory/allocation,
concurrency, installed/root/mode/platform and full P00–P11 acceptance remain open.
The two unregistered scoped-operation counters each refer to the same four PPSVX
scalar rows. They are not additive and do not change the350 registered rows.
All2113 routines and130 missing required XBLAS remain required; no full credit.


## Executed output-alias/placement candidate12

Audit `42add6e1c66a9151ed99e1637c7b65b4fec37b88a6a2699a297ffde97b679845`
and its seal bind27 raw records plus the seal. Six configurations each execute
8 tests, with4 behavioral passes and4 ordinary scalar mathematical failures,
zero skips and no sanitizer diagnostics. Each fault/preflight run passes83896
profiles/802456 checks. Added16384 profiles/81920 checks comprise6912 RCOND
aliases,2048 FACT E output-flag aliases,6912 Device/Managed operand placements
and512 invalid/stale supplied flags. Combined with C11,28928 mutual-argument
alias cases cover7 N,9 E and8 F arguments across all scalars/layouts/triangles.

Views now carry output addresses and supplied flags, defaulting to exactly the
previous fixture objects/values. Numeric aliases use actual containing arrays;
complex components use standard interleaved access. An overlapping enum output
glvalue is formed only for address rejection, with no enum value read or write.
All rejected calls preserve numeric/scratch storage and avoid foreign entry.
All previous assertions, public82176 profiles,25331 counts and4 signatures
remain; each scalar numerical gate still fails44/600 checks.

The two strict failures for unnamed unused Fixture parameters are preserved,
with correction in candidate13. Formatting passes.42 objects are fresh;
exact v26 ASC libraries are reused and foreign archives remain unsanitized.
These28 records bring the ledger to4803. Candidate13 adds calibrated allocation
observation. Protected memory, empty/NRHS0/raw factors, concurrency, installed,
root/mode/platform and full P00–P11 acceptance remain open; no full routine credit.


## Executed allocation candidate13

Audit `a28738679df2dbe7ab8d84439f5e572d0200167758b8bf3b214eea0b0519313b`
and its seal bind27 raw records plus the seal. All six configurations pass
83896 behavioral profiles/802467 checks. Actual calibration observes5 C calls
(malloc/calloc/realloc/aligned_alloc/posix_memalign) and2 C++ calls, with exact
C++ observation enabled in each lane. Every148592 query and83896 execution is
observed, yielding C0/0 and C++0/0. Native entry counts remain16896:12800 injected
and4096 real. The11 new assertions calibrate and reconcile this accounting.
Static ELF wrappers cover linked ASC/LAPACK/BLAS allocation references; shared
runtime internals are outside this observation. No foreign sanitizer credit.

All previous numerical assertions remain. Each configuration has4 behavioral
passes and4 ordinary scalar math failures (44/600 each), zero skips/diagnostics.
54 objects are fresh, including2 unchanged allocation support objects per lane;
exact v26 ASC libraries are reused. C12's unnamed parameters are corrected.
C13 strict analysis now requires direct headers for POSIX posix_memalign and the
allocation capability flag; both failures are retained. Formatting passes.
Candidate14 adds these includes and protected-memory tests, but fails to compile
because its test helper named the memory view incorrectly. Candidate15 uses the
actual MutableMemoryView and narrowly explains stdlib.h's POSIX declaration.
No assertion, test registration or warning policy is weakened.

These28 records bring the ledger to4831. Protected query/stale/local-empty,
positive-order NRHS0, raw factors, boundary/ignored reads, concurrency, installed,
root/mode/platform and full P00–P11 acceptance remain open; no full routine credit.


## Executed protected-memory candidates14–15

Audit `7ce30f123eb61ff4ae2907cba17e63677ee7b5dfee415364374ce6391496302c`
and its seal bind42 raw records plus the seal. Each of six current configurations
passes85944 behavioral profiles/812707 checks, including2048 new profiles and
10240 checks. All nine numeric/output arrays and scratch are real containing
arrays created with nonallocating placement array new in mmap storage.
PROT_NONE protects every array during2048 metadata queries and2048 stale-plan
executions. After restoring access, every element remains unchanged.

Across all four scalars,32 triangle/layout combinations,four FACT modes and
N0/3,NRHS0/2, queries and stale rejection precede any numeric/scratch read or
native entry.1024 N0 executions keep inputs/scratch inaccessible and write
exactly RCOND1,FERR/BERR0 and FACT E flagNone, preserving all guards and correct
local reports/factor authorization. Unused full strides use the actual maximum
ASC extent with real empty spans. Positive-order NRHS0 native execution remains
separate. Calibrated observation still reports zero allocations across150640
queries/86968 executions, exact C++ capability enabled and16896 native entries.

Current two strict checks and formatting pass. Candidate14's six failed builds
and two strict failures remain recorded. Candidate15 fixes the test's memory-view
type and uses a single-line modernize exception for the required POSIX stdlib.h
declaration; cstdlib does not guarantee posix_memalign. Direct allocation-observer
include is present. No public API, warning configuration or assertion changes.
54 current fresh objects and30 historical partial-build objects are bound;
v26 ASC libraries remain reused and foreign code unsanitized. Each current lane
retains4 ordinary scalar math failures (44/600 each), zero skips/diagnostics.

These43 records bring the ledger to4874. Positive-order NRHS0, raw factors,
boundary/ignored reads, concurrency, installed/root/mode/platform and complete
P00–P11 acceptance remain open. All2113 routines/130 missing XBLAS stay required.


## Executed positive-order NRHS0 candidate16

Audit `d40b1409922efcbb15b5274fc429e25c241400091d9869598f3b4dd977bf0538`
and its seal bind27 raw records plus the seal. All six configurations pass86584
behavioral profiles/820387 checks, adding640 profiles/7680 checks. Across all
four scalars,32 triangle/layout choices andfour modes, positive N3/NRHS0 makes
a real native call and computes the factor, E scales and analytic condition1/4.
An extra unscaled FACT F variant uses S length0; the lengthN variant also stays
unread. B/X/FERR/BERR and unused S are PROT_NONE through query, two stale-stride
rejections and actual execution. No error-stage storage is supplied, while
estimator work remains required. Maximum ASC strides become native leading3;
changing either original stride by1 still invalidates the old plan.

All640 calls return full INFO0, correct report/factor authorization and preserve
unused arrays, guards and error scratch. Active factor zeros are checked
numerically; untouched bytes exactly. Query/execution counts152560/88888 and
native17536=12800 injected+4736 real reconcile. Calibration remains5 C/2 C++,
exact_cpp1; every operation observes zero C/C++ allocations.54 objects are fresh;
v26 ASC libraries are reused and foreign code unsanitized. Current strict2 and
formatting pass. Each lane retains4 ordinary scalar math failures, zero skips or
sanitizer diagnostics; the public/count/signature assertions remain unchanged.

These28 records bring the ledger to4902. Raw factors, ignored imaginary/boundary
and output reads, concurrency, installed/root/mode/platform and complete P00–P11
acceptance remain open. All2113 routines/130 missing required XBLAS stay required.


## Executed raw-factor candidate17

Audit `ca4a84c3ec284ee64131a66a2c3f5eb1b7d37e03bf6e098f98dc7a2494fb727c`
and its seal bind27 raw records plus the seal. All six configurations pass93240
behavioral profiles/853155 checks, adding6656 profiles/32768 checks.1536 cases
with negative or pure-imaginary diagonal factors independently recover
A=diag4/9/16 and solution columns1/2, condition1/4 and finite epsilon-scaled
error bounds. First/middle/last/all diagonal choices, both FACT F modes,
all scalars/triangles/layouts apply; phase cases use the two complex scalars.

The other5120 cases put minimum subnormal/maximum finite/Inf/NaN coefficients
at each diagonal or first offdiagonal, plus imaginary-only Inf/NaN in complex
factors. These check query/admission, return from the native call with full
INFO0/N+1, absent factor certification, and preserved inputs/padding. They
provide no accuracy credit for mismatched/nonfinite raw factors. No broad scan,
provider workaround or successful-factor provenance is inserted.

Query/execution/native counts159216/95544/24192 reconcile. Calibration remains
5 C/2 C++, exact_cpp1, and no operation allocations are observed.54 objects are
fresh; v26 ASC libraries are reused and foreign code unsanitized. Both current
strict checks and formatting pass. Each configuration still has4 ordinary
scalar math failures, zero skips or sanitizer diagnostics, and unchanged
public/count/signature assertions. These28 records bring the ledger to4930.
Ignored/boundary/output reads, concurrency, installed/root/mode/platform and
P00–P11 acceptance stay open; all2113 routines/130 missing XBLAS remain required.


## Executed ignored-complex-diagonal candidate18

Audit `516ec56c5dd39c00de1930999f14b703a9d323e82e5af311f16e98979b29fbb8`
and its seal bind27 raw records plus the seal. All six configurations pass93752
behavioral profiles/856227 checks, adding512 profiles/3072 checks. An actual
one-element complex AP array straddles two pages: real4 is accessible and
imaginary19 remains PROT_NONE during query and native execution. Both complex
scalars,32 triangle/layout combinations,four modes andNRHS0/2 run with N1.
FACT E's normal A4 chooses no equilibration; no scaled-AP-write claim is made.
All calls solve4X=B with factor2, condition1 and finite epsilon-scaled bounds.
After access is restored, AP remains4+19i and every other input/guard is intact.

Query/execution/native counts159728/96056/24704 reconcile. Actual C/C++ calibration
remains5/2, exact_cpp1 and zero operation allocations.54 objects are fresh;
v26 ASC libraries are reused and foreign code unsanitized. Both current strict
checks and formatting pass. Each configuration retains4 ordinary scalar math
failures, zero skips or sanitizer diagnostics. Public82176 profiles/count25331/
signatures4 pass unchanged; each scalar required math test still fails44/600.
These28 records bring the ledger to4958. No full routine credit is granted.

The exact next task is candidate19 active packed/full/vector and workspace
boundaries, with actual containing arrays, guard pages at both ends/between
matrix major slices and exact declared workspace minima. The read-only recovery
command in state.json checks this sealed candidate, draft identity, source lock,
active worktree and unchanged release checkout before listing the concrete
implementation inputs. Candidate19 has not been created. Pre-native output-read
observation, concurrency, installed/root registration, mode/platform closure and
all remaining P00–P11 work remain required. The350 registered partial Reference
rows,1763 ledger-not-started rows and20 native implemented-unverified rows remain;
the4 tested external PPSVX rows are unregistered and already within2113.
All130 missing required XBLAS stay visible. No merge/publish/push occurred.
