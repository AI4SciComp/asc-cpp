# Ordinary packed positive-definite source admission

All 32 S/D/C/Z PPTRF/PPTRI/PPTRS/PPSV/PPSVX/PPCON/PPRFS/PPEQU entries are
required and currently unimplemented. The pinned source and existing provider
are unchanged. Both mechanical archive closures contain 127 objects, no
writable static symbols, and cabs/cabsf/memcpy/memset after conditioning the
XERBLA edge. That condition requires source validation and is not blanket
progress, allocation, numerical or full-family admission evidence.

## Executed factor integer helper

Unregistered private `Factor` and a literal Fortran-loop model pass 12,340
checks, strict Clang18 and formatting. One fresh GNU11 Debug C++20/no-exceptions
object links the exact existing Core archive. No fake backing or huge object
is constructed for actual LP64/ILP64 endpoint checks. Audit
`54b52e12fb3779eba41244efaaad2bb96b6fe30dfa24e2bb4b421309cf864006`
and its completion seal bind ten original records, including the original
precision-comparison failure. The draft is preserved in the external
`p05-packed-cholesky-draft-01` directory and frozen counts candidate01.

For p=N*(N+1)/2, the selected upper PPTRF path needs p within the native
integer limit. It calls upper-transpose/conjugate TPSV, whose cursor starts
at one. The raw N*(N+1) initialization in other TPSV branches is not executed.
Lower PPTRF additionally needs p+N-2 within the limit when N>1: the expression
JJ+N-J+1 first evaluates JJ+N, largest at J=N-1. The literal model includes
SPR/HPR cursor increments, DOT/SCAL cleanup and unrolled loops, TPSV packed
cursors and the final DO-control values. Zero order completes locally.

Actual native-count endpoints are 65,535 upper / 65,534 lower for LP64 and
4,294,967,295 upper / 4,294,967,294 lower for ILP64. These are integer-helper
limits; real descriptors separately require representable live typed backing
and caller conversion storage. No claim that such giant arrays were allocated
or passed to a provider is made.

## Precision source differences

Ten PPTRF/selected-BLAS source pairs retain all complete statement hashes.
Seven normalize directly. Three reviewed differences remain explicit: CPPTRF
takes the real component after complex subtraction while ZPPTRF subtracts
real components; SDOT/DDOT reverse two independent zero initializations;
CDOTC/ZDOTC use implicit versus explicit widening of exact zero literals.
The corrected comparison labels reviewed equivalence separately from raw
normalized equality. It does not erase the three original mismatches or
claim floating-environment equivalence. Native arithmetic still needs actual
four-scalar mathematical and failure tests.

## Required implementation semantics

The next task is the typed optional-provider PPTRF path and exact GNU-emitted
signature comparison. Preserve ordinary packed storage, upper/lower equations
U^H U or L L^H (transpose for real), explicit row packing and real-only complex
diagonal reads. Nonpositive pivot INFO must retain partial native output.
Upper failure only writes columns through the failing pivot; lower first-pivot
failure only writes the first diagonal, while later lower steps update trailing
storage. Row publication must preserve those distinctions and ignored future
diagonal imaginary components. Invalid or unwritten INFO must remain a defect.
No provider patch, finiteness scan, implicit densification or fallback is
approved. Every other packed-family contract, complete source/finite progress,
ABI, allocation/mode/platform and full P00-P11 gate remains open.
