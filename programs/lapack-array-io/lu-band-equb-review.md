# P05 GBEQUB4 bounded handoff

Implemented: actual S/D/C/Z GBEQUB through eight public query/execution declarations. Required numerical acceptance remains open for all four routines. Root registration, installed public consumer and integrated package evidence remain pending. No full-mode or P05-completion credit is claimed.

Owner: `feature/lapack-p05-general-band-equb`, based on `455c235f8a0c20e4994a035a6b64b81e9950a453`, with 42 exact GB20/GB8/normal-return-guard dependency files. Recovery searched all live worktrees and durable records and verified branch/path absence before creation. No prior implementation was replaced. The 42 dependency files and their sealed originals remain unchanged. No shared header, registry, schema, manifest, upstream source, root worktree, commit or publication was changed.

Evidence directory: `/home/yicai/AI4SciComp/asc-cpp-evidence/lapack-array-io/p05-general-band-equb-01-xauxfv8x`. `final-source-01.json` binds the 8 owned C++ files; `final-context-01.json` binds the 839-file compilation context. `dependency-preservation-final-01.json` checks all 42 copied and original dependencies. Historical initial builds, strict failures, candidate copies and raw logs remain preserved.

## Source and API contracts

Reference-LAPACK source is the clean exact pin `6ec7f2bc4ecf4c4a93496aa2fa519575bc0e39ca`, with the real LP64 and globally widened ILP64 archives. Source closures bind all 4 root symbols, all 8 archive members per ABI, installed headers, exact source hashes and archive-member hashes. All 8 actual gfortran-11 emitted declarations are checked against the complete installed native prototypes. There are no CHARACTER arguments, hidden lengths, WORK query or numerical workspace for this slice.

The compact `ReferenceGeneralBandView` is unchanged: column-major compact AB, full LDAB*N backing including M=0, arbitrary legal nonnegative KL/KU without clipping, rectangular and empty matrices, host/pinned-host access and ignored source corners. Expanded LU band storage is a separate convention. The source argument check requires LDAB>=KL+KU+1 despite contradictory upstream LDAB prose. Immutable GB20 checked arithmetic bounds source M+1/N+1, J+KL, KU+1+I before subtraction and M+J INFO arithmetic. Formula queries are metadata-only and bind source/scalar/shape/stride/vector/provider identity.

All metadata aliases are checked before report reset. Other preflight failures reset the report but change no numerical/workspace byte and make no native call. Execution initializes full-width INFO to the invalid native minimum after preflight. Negative, unwritten, isolated partial-width and impossible positive/empty INFO are provider defects. Valid positive INFO preserves the raw value, zero-based row/column index and documented partial validity. No factor family is certified. No packing, densification, hidden scratch, fallback, provider-handler replacement or local quantization algorithm is introduced.

Actual radix-power arithmetic and raw statistics are retained. Source AMAX is assigned after row quantization, not from the original maximum. Nonfinite arithmetic and complex CABS1 overflow retain the pinned platform behavior; no blanket finite-output or condition-number improvement promise is added. `source-bound-review.md` records complete arithmetic/runtime admission, including ABI-dependent power helper disassembly and runtime library hashes.

## Executed acceptance

The final exact candidate ran 34 ordinary CTests in each of eight serial configurations: GNU11 Debug and Release, Clang19 ASan+UBSan, and GNU11 libc-allocation probe, each against both real ABIs. Every configuration reports 18 passed, 16 required mathematical failures, 0 skipped, CTest exit 8. Totals: 272 executions, 144 passed, 128 mathematical failures, 0 skips. `final-01-matrix-commands.json`, individual raw logs/JUnit records and `final-01-acceptance-results.json` bind these results. All configurations build successfully. No ASan or UBSan diagnostic occurred. The pinned Fortran archives are uninstrumented; explicit input/output/statistics/scratch guards supplement C++ instrumentation.

Numerical tests retain 27 deterministic compact profiles plus three zero-row/column partial-output cases per scalar and regular/scaled mode, including asymmetric rectangular bands, excess widths, empty shapes and source-ignored NaN corners. They independently check original CABS1 AMAX, finite radix-power scales, safe scale bounds, returned-vector condition ratios and unclamped scaled-column magnitude. Singleton AMAX and extreme cases are additional ordinary gates. Full AB byte immutability, vector/statistics/scratch redzones and allocation checks remain active even in the failing mathematical cases.

All matching-plan INFO, stale source/provider/stride/plan, scale shape/stride/device, operand/scratch alias and non-report metadata alias checks pass. Metadata aliases preserve prior report fields; other structural failures reset them before a proven noncall. Fresh-process first foreign calls and queries pass both C++ and libc allocation probes. Raw native nonfinite controls return normally with guards/noallocation checked; they are explicitly fidelity observations, not finite mathematical guarantees. Every foreign-calling main starts with the unchanged common normal-return guard.

All 12 strict TU/ABI checks pass using the full repository configuration and default header filter: 10 exact unchanged checks from strict02 and 2 final fault-test checks from strict03, bound by `final-strict-01-commands.json`. No warning class was disabled. Both GNU11 and Clang19 compile the public header alone in both ABI contexts. All 8 public declarations have useful Doxygen semantics, with no documentation warnings. All 8 C++ files pass repository formatting. Live Google C++ and Python guide snapshots are recorded; repository C++20, naming, Result/Status and diagnostic linker-hook conventions remain accepted exceptions. No Python file is introduced into the repository.

An external counterfactual removes only INFO sentinel initialization from the exact candidate. Its eight scalar/ABI fault runs all fail actual Status checks for no-write/partial-width returns. This proves the matching-plan regression observes the defect. It is not a historical implementation or an expected-fail setting on required tests. `info-zero-control-commands.json` retains exact source/control hashes and raw evidence.

## Required mathematical failures still open

Every ABI/scalar returns AMAX 2 for nonzero singleton 3, violating the documented original maximum. Complex regular/scaled fixtures also expose this mismatch. Maximum finite input can quantize to infinity. LP64 libgcc power helpers exponentiate before reciprocating; subnormal singleton inputs can become zero maxima and INFO 1. True ILP64 libgfortran helpers invert the base first and retain those subnormals, but singleton ROWCND becomes 8 or much larger although min(R)/max(R)=1. Independent direct pinned singleton tests reproduce all these classes. All 16 failing test names remain in each ordinary CTest run, without suppression, tolerance weakening, waiver, expected-fail conversion, source patch or fallback.

## Root integration requirements

Import only the 8 new C++ files and this review on the exact GB20 dependencies. Add the new public header to the optional reference-provider header/ownership/documentation generators and add `reference_lu_band_equilibration_radix.cc` to its source target. Register only `sgbequb`, `dgbequb`, `cgbequb`, `zgbequb`; GBEQU is distinct. Preserve implementation versus verification state and all required failing mathematical gates. The external `harness-final-01/CMakeLists.txt` shows the 34 additive/reused test registrations, linker-wrapper hooks and exact compiler-emitted ABI-header definition; root should use repository registration conventions. The pure source-count test is the existing unchanged GB20 dependency and need not be duplicated.

Root owns public installed-consumer, integrated no-exceptions/isolation/package and source/registry closure revalidation on its final integration identity. No installed or full-package claim is made here. Next bounded action is root review of the sealed manifest/patch and registration on its integration branch; mathematical acceptance remains blocked by the unchanged required pinned-source failures.
